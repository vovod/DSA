#include <bits/stdc++.h>
//#include <iostream>
//#include <map>
//#include <vector>
//NHDvovod
using namespace std;
typedef long long ll;
int main(){
    int n;
    cin >> n;
    while(n--){
        int s, t;
        cin >> s >>t;
        int a[20001] ={0};
        queue<int> q;
        q.push(s);
        while (a[t]==0)
        {
            int x = q.front();
            q.pop();
            if(x-1 > 0 && a[])
        }    
    }
}